# 创建该文件的LLM大模型名称：Arena.ai Agent Mode
# 创建时间（北京时间，精确到秒）：2026-05-29 17:30:00 CST

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "soundproof-agent"
version = "0.1.0"
description = "隔音窗专家咨询与购物辅助系统（Phase 1 购物主链路开发中）"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "pydantic>=2.8.2",
  "pyyaml>=6.0.2",
  "rich>=13.7.1",
  "typer>=0.12.3",
]

[project.optional-dependencies]
phase1 = [
  "playwright>=1.54.0",
]
web = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "jinja2>=3.1.4",
  "playwright>=1.54.0",
]